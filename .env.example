# Copy to .env (gitignored) and fill in. Used by scripts/substack_push_draft.py
# Set a Substack password first: substack.com -> Settings -> Account -> Set password
SUBSTACK_EMAIL=athro27@gmail.com
SUBSTACK_PASSWORD=

# Fallback if password login hits a captcha: paste your browser cookie string
# (DevTools -> Application -> Cookies -> substack.com -> copy substack.sid etc.)
# SUBSTACK_COOKIES_STRING=
